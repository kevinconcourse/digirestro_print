name: digirestro_print
description: A new Flutter package project.
version: 0.0.2

environment:
  sdk: ">=2.18.0 <3.0.0"
  flutter: ">=1.17.0"

dependencies:
  flutter:
    sdk: flutter
  blue_thermal_printer: ^1.2.3
  # esc_pos_printer: ^4.1.0
  flutter_blue_plus: ^1.8.7
  ping_discover_network_forked: ^0.0.1
  flutter_thermal_printer: ^1.2.1+1
  digirestro_esc_pos_utils:
    git:
      url: https://github.com/kevinconcourse/digirestro_esc_pos_utils
      ref: usb_changes

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.0

# flutter:
  # Uncomment and update if you add assets or fonts
  # assets:
  #   - images/a_dot_burr.jpeg
  #   - images/a_dot_ham.jpeg
  #
  # fonts:
  #   - family: Schyler
  #     fonts:
  #       - asset: fonts/Schyler-Regular.ttf
  #       - asset: fonts/Schyler-Italic.ttf
  #         style: italic
