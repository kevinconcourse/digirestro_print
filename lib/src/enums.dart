enum PrinterType { bluetooth, lan, imin, sunmi, usb }

//TEST
enum ConnectionStatus {
  timeout,
  connected,
  printerNotSelected,
  ticketEmpty,
  printInProgress,
  scanInProgress,
  disconnect,
}
